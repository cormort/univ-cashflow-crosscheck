# 勾稽底稿的規則

這份整理的是「大學現金流量表勾稽底稿」本身的規則——表怎麼運作、代號怎麼編、
來源怎麼認。程式的用法看 [README.md](README.md)。

標記說明：**〔實測〕**= 在 113/114/115/116 四個年度的實際檔案上驗證過；
**〔表述〕**= 底稿自己的欄位說明寫的；**〔推論〕**= 依資料推斷、沒有文件佐證。

---

## 一、表的結構

一列同時放**兩份互不相干的清單**，左半邊平衡表、右半邊現金流量表。兩份清單
各自獨立，只是並排在同一列上。

| 欄 | 內容 | 從哪來 |
|---|---|---|
| A | 平衡表科目 | 來源檔中**縮排 ≤ 2 層**（全形空白）的科目 〔實測〕 |
| H | 現金流量表項目 | 來源檔**全部**項目，1:1 〔實測〕 |

47 張明細表**逐格完全相同**，只有 `L1`（校序號 1–47）不同：

```
L1 = 校序號
K1 = L1 + 2      區塊裡 A=科目、B=合計、C=第1校，所以第 n 校在第 n+2 欄
所有查表公式都寫 $K$1
```

改一格 `L1` 整張表就切換成另一所學校。五份原始資料只寫一份在總表骨架下方，
明細表用 VLOOKUP／OFFSET 指過去。

---

## 二、勾稽怎麼運作

欄位定義取自底稿自己的說明列（第 4、6、7 列）〔表述〕：

```
B   上年 12/31 預計數                    ← VLOOKUP 上年區塊
C/D 調整項目「借方」（代號、金額）          ← 人工填
E/F 調整項目「貸方」（代號、金額）          ← 人工填
G   本年 12/31 = B欄期初數 + C至F欄調整項目
        資產：    G = B + D − F
        負債淨值： G = B + F − D
L   教育部「平衡表」期末數                 ← VLOOKUP 本年區塊

H   現流項目        I  該項目的代號
J   教育部「現流表」原始資料               ← OFFSET 現流區塊
K   「自己拉現流表公式」
      K = −SUMIF(C欄找 I, D欄) + SUMIF(E欄找 I, F欄)
```

**串起兩邊的是 `I` 欄那個代號**：平衡表側在 C／E 掛上代號，現流側在 I 掛上
同一個代號，`K` 的 SUMIF 就把所有掛該代號的借貸金額加總，反算出「從平衡表
推出來的現流數」。

### 三個檢查點

| 欄 | 公式 | 意義 |
|---|---|---|
| **M** | `L − G` | 自行調整後的期末數 vs 教育部平衡表期末數 |
| **P** | `J − K` | 現流表原始數 vs 由調整反算的數 |
| O／Q | `L − B` vs `D − F` | 教育部的比較增減 vs 自行調整的借貸淨額 |

**M 和 O／Q 其實是同一件事**：`G = B + D − F` ⇒ `Q = D − F = G − B`，
而 `O = L − B`，所以 `O − Q = L − G = M`。M 歸零 ⟺ O 等於 Q。

**條件式格式就是這件事的視覺化**：`J ≠ K` 標色、`M ≠ 0` 標色。
全部不標色就是勾稽通過。這不是裝飾，是功能。

### 實際例子

```
第 9 列  A=流動金融資產   C=231  D==J50*-1   E=211  F==J42
第 42 列 H=減少流動金融資產  I=211
第 50 列 H=增加流動金融資產  I=231
```

現流表說「增加流動金融資產」是 `J50`（資產增加＝現金流出，負數），
所以借方 `D9 = −J50`；「減少流動金融資產」是 `J42`（現金流入），
所以貸方 `F9 = J42`。反過來 `K50 = −SUMIF(C欄找231, D欄) = −D9 = J50`，
於是 `P50 = J50 − K50 = 0`。

---

## 三、代號規則

格式：**活動別 1 碼 + 群 2 碼 + 群內序 2 碼**，例如 `10401` =
業務活動／折舊、減損及折耗／土地改良物。

| 首碼 | 活動別 |
|---|---|
| 1 | 業務活動之現金流量 |
| 2 | 投資活動之現金流量 |
| 3 | 籌資活動之現金流量 |
| 4 | 不影響現金流量之投資與籌資活動 |
| 9 | **設計上**就沒有現流項目可對應（不涉及現金的平衡表科目互轉），目前只有 `90003` |
| A | 本期賸餘（短絀） |

上表寫的是活動別的名字；`gen_master()` 實際比對的字串**帶 `▼` 前綴**
（[build_gouji.py](build_gouji.py) 的 `CF_SECTIONS`）。用 `--master` 換公版時，
若新公版的節名沒有 `▼`，四大活動別會全部認不出來——換版後請先確認代號數量沒有暴跌。
| B | ▼現金及約當現金之淨增（淨減） |

規則：

- **群 = 上級科目**，依艾富公版科目表的順序編號
- **只有葉節點有代號**，群本身（折舊、攤銷、調整項目…）沒有
- 對著**公版**編號，不是對著當年度的清單

最後一點是關鍵。舊制對著當年度清單編，某年缺一項後面就整組往上擠——
實測 113→114 因此改動了 30 個代號（`固定資產之增置 › 土地改良物` 從 `252`
變 `251`，因為 114 的清單少了「土地」那一列）〔實測〕。對著公版編，槽位固定，
缺項不位移。

三位數裝不下公版（「不影響現金流量」那節有 50 個群、最大群 33 項），
所以放寬到 5 碼。

### 9xxxx 這一類

`9xxxx` 只給**設計上就沒有現流項目**的調整——目前只有一個：
`90003`（原 `73`，累積短絀 貸方），不涉及現金、純粹平衡表科目互轉。

**注意這裡曾經判斷錯誤。** 原本 `121`（土地 借方）和 `321`（什項負債 借方）
也被歸進 9xxxx，因為它們在 116 世代的範本裡確實查不到對應的現流項目。
但拿 113/114 的完成檔一比就發現，它們的金額公式指得很明確：

```
114  土地   借方  代號121  金額 =-J28    → 「其他 › 土地」
114  什項負債 借方  代號321  金額 =J80*-1  → 「減少其他負債」
```

它們有真正的目標，只是那兩個現流項目**在 115/116 的來源裡不存在**，
代號那條線就斷了（I 欄改號後 C/E 沒跟著改），只剩公式那條線還在。

**判準**：「這一年沒有對應項目」和「設計上就沒有對應項目」是兩件事。
前者該保留配對、讓它在有該項目的年度自動生效；後者才歸 9xxxx。
分不出來時，去翻有那個項目的年度的完成檔——金額公式會說話。

---

## 四、來源檔的識別

**一律認結構、不認工作表名字**——表名逐年是 `細修0814`／`細`／`明細`／`Sheet1`。

| 要找的 | 判斷方式 |
|---|---|
| 本年平衡表 | `A1` = `1.本年預算數` |
| 上年平衡表 | `A1` = `5.上年調整後預算數` |
| 現金流量表 | 第 4 列 C~AW 有 47 個校名 |

同時符合多張時，取名稱帶最新四位日期後綴者，同日期取名稱最短者
（避開 `114調後2`、`細修2` 這種暫存版）。

骨架列數跟著當年度來源走：來源沒有的科目刪列、多出來的補列。
順序以骨架為準——人工把「什項負債」排在「遞延負債」前面是有意的，
照來源順序重排會把它底下整組調整列拆散。

---

## 五、範本裡既有的缺陷（都已在程式端修正）

這些是歷年人工編輯累積的損壞，記在這裡以免日後又被當成「本來就該這樣」。

| 問題 | 規模 |
|---|---|
| 檢查欄 M/O/P/Q 被拖曳拖歪（`O50=L50-B49`、`M133=L135-G137`） | 範本 82 格 → 每份產出 3713 格 |
| 條件式格式 47 張表有 12 種形狀、19 條規則比對 `#REF!`（死規則）、`M94:M96` 破洞 | 全部 47 張 |
| K 欄 SUMIF 貸方範圍不一致（7 格 `$E$7:$E$135` vs 57 格 `$E$7:$E$127`） | 7 格 |
| B/L 的 VLOOKUP 查找值指到別列（`L131=VLOOKUP(A132,…)`） | 數格 |
| H 欄縮排漂移，若干項目掛錯父項 | 4 筆配對受影響 |

程式現在會逐列重新生成檢查欄、統一條件式格式、對齊 SUMIF 範圍。
**這些不會自動修好範本檔案本身**，只是每次產出時重新生成正確版本。

---

## 六、還沒解開的

1. **舊代號第 2 碼的來源。** 公版位置怎麼算都對不上——「折舊」是公版第 3 個
   子項卻編 `10`，「其他」和「流動資產淨減」之間空了 `14`。可能來自更早的
   標準科目表。改用新制後已不影響。
2. **「填完調整分錄後 M/P 應該歸零」** 只在歷年人工完成檔上驗證過
   （歸零率 99.9%）〔實測〕，不是規則的明文。
3. **本年平衡列不歸零是正常的**（調整分錄還沒填）；**上年平衡列必須 47 校
   全部為 0**，那是純從來源查表得到的數，不平衡就代表資料沒正確搬進去。

---

## 七、用別的勾稽表擴充配對表

### 先看清楚分母：能擴充的空間比想像中小得多

一度以為配對表「只覆蓋公版的 26%」（67／258），還有 191 個缺口。**那個分母是錯的。**

配對表的鍵是 `科目\t借貸\t第N筆`，而 `第N筆` 數的是**範本自己第 N 個調整槽**
（[build_gouji.py](build_gouji.py) 的 `seen[(owner, side)]`，走樣板列序）。所以一筆配對
只有在「範本的那個科目真的有第 N 個調整槽」時才會生效。實測範本：

| | 數量 |
|---|---|
| 範本的調整槽總數 | 104 |
| 已有配對 | **104（100%）** |
| 尚未配對 | **0** |

配對表另有 4 個鍵指到範本沒有的槽（`遞延資產 借2`、`遞延資產 貸2`、`遞延負債 借3`、
`特別公積 借1`），是舊世代留下來的，永遠不會生效——無害，但也不算覆蓋。

**沒有缺口可補。**「公版 258 項」是代號主檔的規模，不是配對表該對齊的目標；
那 191 項是這些學校沒發生過的交易，沒發生就沒有調整槽，沒有槽就沒地方放配對。

新配對只有在**範本長出新的調整槽**時才需要——也就是某所大學真的做了新型態的調整分錄。
那是來源檔驅動的，蒐集別人的勾稽表**創造不出槽**。

### 需要什麼

**只要那份勾稽表本身**，不需要它的來源檔（平衡表／現流表的數字已經在表裡）。

必要條件是版面要有「左邊平衡表調整、右邊現流項目」的雙欄結構：

```
C/E 借貸代號   D/F 借貸金額     ←→     I 現流代號   J 現流數
```

**右半邊在第幾欄不固定**〔實測〕。左半邊的 C/D/E/F 各家一致，右半邊會被中間插進去
的欄推著走——115 年度七份他機關勾稽表裡，五份在 H、醫療藥品多一欄「平衡表數」
推到 I、高中多「平衡表數＋檢誤」推到 J。`extract_pairs.py` 認第一節的節名
「業務活動之現金流量」定位，代號與金額固定接在右邊兩欄。

他機關多半還是 `.xls`（BIFF8），openpyxl 讀不了，`extract_pairs.py` 會自動用
LibreOffice 轉一份暫存 xlsx，直接餵 `.xls` 即可。

**代號跟我們不一致沒關係**——配對表是用**名稱**當鍵，代號只是查表產生的結果。
實測我們自己的代號都不穩定（113→114 改了 30 個），所以別人用什麼編碼都行，
只要**該檔案內部自洽**（C/E 掛的號碼跟它自己的 I 欄對得起來）。

**最好是已填完調整分錄的完成檔**：空白表也抽得出配對（代號和公式都在），
但填完的可以額外跑歸零檢定，用 `P = J − K` 是否趨近 0 來**驗證**配對正確，
而不只是照抄。我們這次就是靠這招驗證了 76 筆，也是靠它抓出 121/321 被歸錯類。

### 流程

```bash
python extract_pairs.py 別人的勾稽表.xlsx [輸出.csv]
```

**只讀不改**，會報告三件事：

1. **名稱與公版的重疊率** → 100% 就能直接併；不到就要先做名稱對照
2. **能補多少缺口** → 用到哪些我們沒配對過的現流項目
3. **配對來源** → 由金額公式推得（可交叉驗證）還是只有代號

然後才決定要不要併。併之前建議跑歸零檢定確認。

### 三個要注意的地方

1. **基金別的會計處理可能不同。** 校務基金、醫療作業基金、其他特種基金未必
   一致。新來源的配對應該**標註出處、當候選看待**，不要直接合併——
   `extract_pairs.py` 的輸出已經帶 `來源` 欄。
2. **同一科目可能有多種合法配對**，取決於交易性質。鍵是
   `(科目, 借貸, 第N筆)` 支援多筆，但更多來源會冒出更多變體，
   要人工判斷哪些是真的並存、哪些是別人做錯。
3. **價值只在印證，不在補缺口。** 承上面的分母：範本 104 個調整槽已經全部有配對，
   別人的勾稽表補不進新的。所以拿到新來源時該問的是「有沒有**牴觸**我們既有的配對」，
   不是「能不能多補幾項」——後者已經證實沒有空間。
   要找牴觸，**優先拿我們自己的歷年完成檔**（113/114 那種），不是他機關的：
   上面 121/321 被歸錯類就是這樣抓到的（見第三節），而跨基金別的科目體系不同，
   連比對的共同基礎都沒有。

### 實測：115 年度七份他機關勾稽表

榮民醫療、社教機構、農作、農田水利、醫療藥品、陽交大附醫、高中——共抽出 418 筆配對，
涉及 113 個相異現流項目名，但**只有 15 個命中公版**，扣掉已有配對後**真正能補的缺口 7 個**：

```
利息收入   收取利息   增加公積   增加其他資產   增加短期墊款   減少其他資產   減少長期應收款
```

命中率低不是抽取壞掉，是上面第 1 點的具體樣貌：這些基金的現流表根本是**另一套科目表**。
它們把折舊寫成一行「(1)折舊」，公版則按資產別拆成土地改良物、房屋建築…十幾項；
「本期賸餘」「流動資產淨減(淨增－)」這類彙總列公版也沒有。去掉 `(1)`／`1.` 之類的
編號前綴後仍然對不上，**不是格式問題，是會計科目體系不同**——要併就得先做名稱對照表。

後續追查那 7 項時發現**一項也併不進去**，這才回頭把上面的分母改對。逐鍵查範本的槽位：

| 候選鍵 | 範本狀況 |
|---|---|
| `應收款項 借2`、`應收款項 貸4`、`什項資產 貸2`、`什項資產 貸4`、`長期應收款 貸1` | ✗ 範本沒有那個槽，填了也不會生效 |
| `資本公積 貸2` | ⚠ 已是 `提存(撥用-)公積` |
| `遞延資產 借1` | ⚠ 已是 `增加遞延費用` |
| `短期貸墊款 借1` | ⚠ 已是 `流動資產淨減（淨增）` |

另外 13 個來源科目裡有 6 個（`存出保證金`、`暫付及待結轉`、`催收款項`、`應收利息`、
`受託經營權益`、`備呆-催收`）**大學平衡表根本沒有這個科目**。

也就是說這 7 項不是「還沒併」，是**併不得**：五個石沉大海，三個會把既有正確配對覆蓋成
基金別的語意。結論不是「跨基金別幫助有限」，而是**這條路整條是死的**——
配對表已經 100% 覆蓋範本的槽，蒐集再多別人的勾稽表都不會多出一個槽。

---

## 八、知識存在哪裡

| 檔案 | 內容 | 性質 |
|---|---|---|
| `現流代號主檔.json` | 現流項目 → 代號 | 由艾富公版機械產生，`--master` 可重跑 |
| `勾稽配對表.json` | 平衡表科目調整 → 對應的現流項目（107 筆） | **累積的專業判斷，重建不出來** |
| `template.xlsx` | 骨架、格式、調整欄的公式 | 已清掉所有年度金額 |

`勾稽配對表.json` 是這個專案裡最有價值也最脆弱的檔案。有異動就 commit，
讓變更歷史留在 git 裡。
